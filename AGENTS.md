# Repo Instructions

- Avoid exposing internal framing labels like `portfolio`, `hero build`, or similar meta-design terminology in user-facing copy.
- In UI text, prefer the direct subject itself: project names, GitHub, site, work, writing, contact, experience.
